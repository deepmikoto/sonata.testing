{% extends '@App/sonata_page_layout.html.twig' %}

{% block extra_css %}
    <link href="https://fonts.googleapis.com/css?family=Open+Sans:300italic,400italic,600italic,700italic,800italic,400,300,600,700,800" rel="stylesheet" type="text/css">
    <link href="{{ asset('bundles/sonatacore/vendor/components-font-awesome/css/font-awesome.min.css') }}" rel="stylesheet" type="text/css">
    {% stylesheets 'bundles/app/css/*' filter='cssrewrite' %}
        <link rel="stylesheet" href="{{ asset_url }}" />
    {% endstylesheets %}
{% endblock %}

{% block sonata_page_content %}
    {{ sonata_page_render_container('header', page) }}
    <div class="container">
        <div class="row">
            {{ sonata_page_render_container('content_top', page) }}
        </div>
        <div class="row">
            {{ sonata_page_render_container('content', page) }}
        </div>
        <div class="row">
            {{ sonata_page_render_container('content_bottom', page) }}
        </div>
        <footer class="row">
            {{ sonata_page_render_container('footer', page) }}
        </footer>
    </div>
{% endblock %}

{% block extra_js %}
    {% javascripts
        '@AppBundle/Resources/public/js/core.js'
    %}
        <script type="application/javascript" src="{{ asset_url }}"></script>
    {% endjavascripts %}
{% endblock %}